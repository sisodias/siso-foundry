You are arm B (SCRATCH) in a controlled experiment. Build ONE file and stop.

TASK SPEC (read it exactly):
---
Build an ES module `webhook.mjs` exporting one function:
  export function createReceiver({ dbPath, secret, schema }) -> { handle, close }

handle({ rawBody, signatureHeader, idempotencyKey }) must:
 1. VERIFY HMAC: signatureHeader is "sha256=<hex>". Compute HMAC-SHA256 of the
    RAW request body bytes keyed by `secret`. If mismatch -> return
    { status:401, reason:'bad_signature' }. The compare MUST be constant-time
    and MUST NOT throw on a length mismatch.
 2. VALIDATE: parse rawBody as JSON then validate against `schema` (a JSON-Schema
    object). On bad JSON or schema failure -> { status:400, reason:'invalid_body' }.
    Signature is checked BEFORE parsing.
 3. DEDUPE by idempotencyKey: if seen before, do NOT persist again; return
    { status:200, reason:'duplicate', replayed:true, id:<same id as original> }.
    Must be race-safe (atomic, no check-then-act gap).
 4. PERSIST on first valid delivery: store (idempotency key, raw body, generated
    id) durably in SQLite at dbPath. Return
    { status:200, reason:'accepted', replayed:false, id:<recordId> }.
 close() closes the DB.
 ES module, Node 22, no network.
---

You have NO cards and NO web. Use only your own training knowledge and Node
built-in modules. You may pick any npm libraries you remember; declare them.

DELIVERABLES (write exactly these files, nothing else):
1. /ARMDIR/webhook.mjs  — the working module.
2. /ARMDIR/deps.json    — JSON object of npm deps you import (only what you import).
3. /ARMDIR/NOTES.md     — log every place you were UNSURE / working from memory
   (especially HMAC verification and idempotency), as honest bullets.

Write the files with the Write tool. Do not run npm or tests. Stop when written.
